<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#43464f"
                android:centerColor="#43464f"
                android:centerY="1.0"
                android:endColor="#43464f"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#64ca32"
                    android:centerColor="#64ca32"
                    android:centerY="1.0"
                    android:endColor="#43464f"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>